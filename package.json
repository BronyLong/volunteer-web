{
  "name": "volunteer-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n SERVER,WEB -c blue,green \"npm --prefix server run dev\" \"npm --prefix react-volunteer-app run dev\"",
    "start": "concurrently -k -n SERVER,WEB -c blue,green \"npm --prefix server run start\" \"npm --prefix react-volunteer-app run dev\"",
    "install:all": "npm install && npm --prefix server install && npm --prefix react-volunteer-app install",
    "coverage": "npm run coverage --prefix react-volunteer-app && npm run coverage --prefix server",
    "test": "npm test --prefix react-volunteer-app && npm test --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}